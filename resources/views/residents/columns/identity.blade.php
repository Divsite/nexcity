<div class="d-flex flex-column">
    <span class="fw-semibold">{{ $row->name }}</span>
    <span class="small text-muted">{{ $row->email }}</span>
    <span class="small text-muted">{{ '@' . $row->username }}</span>
</div>
